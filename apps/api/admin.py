# The default DRF TokenAdmin is used without modification.
# Admin tokens are created via Admin → Auth Token → Tokens → Add Token.
# Tokens are scoped to is_staff users only (enforced in apps/api/permissions.py).
